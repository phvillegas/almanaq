<!--
  A back arrow. Drawn here rather than pulled from material-icons for the same reason as
  the other icons: five of them still do not justify shipping the whole set, and
  CLAUDE.md rule 5 gates dependencies that ship.

  `autoMirrored` is set even though v1 ships only Spanish and English: a back arrow that
  points the wrong way in a right-to-left locale is the kind of thing nobody remembers to
  come back for, and the attribute costs nothing today.

  Tinted by the caller, so the fill is left as the standard black placeholder.
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24"
    android:autoMirrored="true">
    <path
        android:fillColor="@android:color/white"
        android:pathData="M20,11H7.83l5.59,-5.59L12,4l-8,8 8,8 1.41,-1.41L7.83,13H20v-2z" />
</vector>
